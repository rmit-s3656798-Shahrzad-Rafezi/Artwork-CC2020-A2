<?php
session_start();
	$username = $_POST['id'];
	$password = $_POST['password'];

	// TODO: connect to the db server

	// TODO: define the query

	// TODO: run an insert query

    // TODO: check to see if that user exist in database
    if(true) {

    }
    else {
        print"<h3>You are not registered</h3>";
    }

?>